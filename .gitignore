# cache local de l'outil de revue design (la config sanctionnée, elle, est versionnée)
.impeccable/hook.cache.json

# dépendances du build hors ligne : le lockfile est versionné, pas les paquets
tools/node_modules/
